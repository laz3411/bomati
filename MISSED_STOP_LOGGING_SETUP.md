# Google Sheet 무정차 기록 연동

이 연동은 Roblox가 감지한 무정차 사건을 Firebase의 `operations/missedStops/{CompanyId}`에 회사별로 분리해 보존하고, 각 회사의 Google Sheet가 자기 `CompanyId` 경로에서만 새 사건을 가져오는 방식입니다. Google 계정 비밀번호나 서비스 계정 키를 HTML·Roblox 코드에 넣지 않습니다.

회사마다 스프레드시트를 복사한 뒤 같은 `google_apps_script.gs`를 그대로 붙여넣어 사용할 수 있습니다. 각 복사본의 Apps Script 설정과 자동 실행 트리거는 서로 독립적으로 저장됩니다.

## 최초 1회 설정

1. 회사용 Google Sheet를 새로 만들거나 BOMATI 회사용 원본 시트를 복사해 엽니다.
2. 상단 메뉴에서 **확장 프로그램 → Apps Script**를 엽니다.
3. 기본 편집기 내용을 지우고 이 저장소의 `google_apps_script.gs` 전체를 붙여넣은 뒤 저장합니다.
4. 함수 선택 목록에서 `prepareCompanySheet`를 선택하고 **실행**합니다.
5. Google 권한 확인 화면에서 현재 시트 권한을 승인합니다.
6. Google Sheet로 돌아오면 첫 번째 탭 `무정차 기록`, 두 번째 탭 `회사 설정`이 생성됩니다.
7. 두 번째 탭의 `B3`에 회사명을 입력합니다.
8. 상단 **BOMATI → 회사 식별 아이디 생성**을 눌러 `B4`에 회사 식별 아이디를 만듭니다.
9. Roblox Studio에서 해당 회사의 각 Bus Model을 선택하고 아래 두 Attribute를 추가합니다.
   - `CompanyName`: **String**, 회사 설정 탭 `B3` 값
   - `CompanyId`: **String**, 회사 설정 탭 `B4` 값
10. Google Sheet에서 **BOMATI → 무정차 기록 연동 시작**을 누릅니다. 이후 1분마다 해당 회사 기록만 자동 동기화됩니다.

즉시 확인하려면 시트의 **BOMATI → 지금 기록 가져오기**를 누릅니다. 자동 동기화를 다시 설정하려면 **BOMATI → 무정차 기록 연동 시작**을 누릅니다.

## 시트에 기록이 들어오지 않을 때

1. Apps Script 편집기의 코드를 저장소 최신 `google_apps_script.gs` 전체 내용으로 교체하고 시트를 새로고침합니다.
2. **BOMATI → 연동 테스트 기록 생성**을 누릅니다.
3. 첫 번째 탭 마지막 행에 `연동 테스트 차량`이 즉시 표시되면 Google Sheet와 회사별 Firebase 경로 연동은 정상입니다.
4. Roblox Studio의 실제 운행 버스 최상위 Bus Model에 `CompanyName`, `CompanyId`가 모두 **String Attribute**로 들어갔는지 확인합니다. 값은 두 번째 탭 `B3`, `B4`와 공백까지 동일해야 합니다.
5. Roblox Output에 `[회사별 시트 연동 비활성]` 경고가 표시되면 뒤에 나온 속성 오류를 수정합니다.

실제 운행 기록은 하차벨을 누른 순간이 아니라, 하차벨이 켜진 채 정류장을 정상 정차 없이 완전히 통과한 순간 생성됩니다. 단순 벨 입력 여부는 무정차 기록 대상이 아닙니다.

`CompanyId`는 `company_`와 UUID 32자로 생성됩니다. 회사 구분용 라우팅 값이며 실제 로그인 비밀번호나 보안키가 아닙니다. 이미 기록을 사용 중인 회사가 식별 아이디를 다시 생성하면 기존 아이디 경로의 기록이 새 시트로 들어오지 않으므로, 재생성 확인창을 읽고 진행해야 합니다.

## 무정차 판정 기준

- 버스가 정류장 75 studs 안으로 접근한 동안 일반 또는 장애인 하차벨이 켜져 있어야 합니다.
- 버스가 정류장 45 studs 안에서 1.25초 이상 2.5 studs/s 이하로 정차하거나 문을 열면 정상 정차로 처리합니다.
- 정상 정차 없이 정류장이 진행 방향 뒤로 넘어가고 58 studs 이상 멀어지면 한 건을 기록합니다.
- 순간 위치 속도가 180 studs/s를 넘는 비정상 좌표 점프는 판정에서 제외합니다.
- 같은 버스·정류장은 한 번 통과한 뒤 45초 동안 중복 기록하지 않습니다.

기록에는 회사명·식별 아이디, 발생 시각, 노선, 차량, 고상·저상, 운전자 Roblox 계정, 정류장 고유번호, 벨 종류와 입력 사유, 최단거리, 통과속도, 서버 세션이 포함됩니다. 회사 담당자는 `검토상태`, `조치내용`, `담당자` 열을 직접 관리할 수 있습니다.
